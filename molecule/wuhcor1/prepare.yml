---
- name: Ensure UCSC Genome Browser database directories exist
  hosts: all
  become: true
  tasks:
    - name: Create UCSC Genome Browser database directory
      ansible.builtin.file:
        path: /gbdb/
        state: directory
        owner: root
        group: root
        mode: "0755"
